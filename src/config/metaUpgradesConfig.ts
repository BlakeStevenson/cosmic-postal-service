/**
 * Meta-Upgrades Configuration
 *
 * This file contains all numeric properties for meta-upgrades (permanent upgrades
 * purchased with stamps). These persist through normal prestige and provide
 * incremental bonuses that can be leveled up to a maximum level.
 */

// === ENHANCED ERGONOMICS ===

/** Base cost for first level of Enhanced Ergonomics */
export const META_CLICK_POWER_BASE_COST = 1;

/** Cost multiplier per level for Enhanced Ergonomics */
export const META_CLICK_POWER_COST_MULTIPLIER = 2.5;

/** Maximum level for Enhanced Ergonomics */
export const META_CLICK_POWER_MAX_LEVEL = 20;

/** Percentage bonus per level (see gameBalance.ts for META_CLICK_POWER_BONUS_PER_LEVEL) */
// Effect: +5% click power per level

// === AUTOMATION PROTOCOLS ===

/** Base cost for first level of Automation Protocols */
export const META_AUTO_POWER_BASE_COST = 1;

/** Cost multiplier per level for Automation Protocols */
export const META_AUTO_POWER_COST_MULTIPLIER = 2.5;

/** Maximum level for Automation Protocols */
export const META_AUTO_POWER_MAX_LEVEL = 20;

/** Percentage bonus per level (see gameBalance.ts for META_AUTO_POWER_BONUS_PER_LEVEL) */
// Effect: +5% automatic production per level

// === BULK DISCOUNTS ===

/** Base cost for first level of Bulk Discounts */
export const META_CHEAPER_BUILDINGS_BASE_COST = 2;

/** Cost multiplier per level for Bulk Discounts */
export const META_CHEAPER_BUILDINGS_COST_MULTIPLIER = 3;

/** Maximum level for Bulk Discounts */
export const META_CHEAPER_BUILDINGS_MAX_LEVEL = 10;

/** Percentage reduction per level (see gameBalance.ts for META_COST_REDUCTION_PER_LEVEL) */
// Effect: -2% building costs per level

// === PASSIVE OPERATIONS ===

/** Base cost for first level of Passive Operations */
export const META_OFFLINE_TIME_BASE_COST = 3;

/** Cost multiplier per level for Passive Operations */
export const META_OFFLINE_TIME_COST_MULTIPLIER = 3;

/** Maximum level for Passive Operations */
export const META_OFFLINE_TIME_MAX_LEVEL = 10;

/** Minutes of offline production per level (see gameBalance.ts for META_OFFLINE_TIME_PER_LEVEL) */
// Effect: +30min offline production per level

// === RESEARCH EFFICIENCY ===

/** Base cost for first level of Research Efficiency */
export const META_RESEARCH_SPEED_BASE_COST = 2;

/** Cost multiplier per level for Research Efficiency */
export const META_RESEARCH_SPEED_COST_MULTIPLIER = 2.8;

/** Maximum level for Research Efficiency */
export const META_RESEARCH_SPEED_MAX_LEVEL = 15;

/** Percentage bonus per level (see gameBalance.ts for META_RESEARCH_SPEED_PER_LEVEL) */
// Effect: +10% research points per level

// === NETWORK EXPANSION ===

/** Base cost for first level of Network Expansion */
export const META_NETWORK_EXPANSION_BASE_COST = 3;

/** Cost multiplier per level for Network Expansion */
export const META_NETWORK_EXPANSION_COST_MULTIPLIER = 3;

/** Maximum level for Network Expansion */
export const META_NETWORK_EXPANSION_MAX_LEVEL = 10;

/** Percentage bonus per level (see gameBalance.ts for META_NETWORK_EXPANSION_PER_LEVEL) */
// Effect: +3% to all production per level
